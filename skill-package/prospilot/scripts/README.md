# ProsPilot scripts (Managed)

No executable scripts run inside Anvita Managed sandbox.

On-chain actions and live security scans happen only in the web app:
https://pharos-agent-pi.vercel.app/chat

This folder exists to satisfy the Pharos skill-package layout (`scripts/`).
